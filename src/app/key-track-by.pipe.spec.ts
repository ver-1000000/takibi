import { KeyTrackByPipe } from './key-track-by.pipe';

describe('KeyTrackByPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyTrackByPipe();
    expect(pipe).toBeTruthy();
  });
});
